{
  "name": "serve",
  "description": "'serve' flow adapted for the command line interface, with an initial wizard to inquire the parameters",
  "steps": {
    "serve-configure": {},
    "install": {
      "type": "flow"
    },
    "build": {
      "type": "flow"
    },
    "launch": {
      "type": "flow"
    }
  }
}
